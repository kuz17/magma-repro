if Marabou takes too long for you :  use this model with 15 epoch training  model = Sequential([     Input(shape=(28, 28)),     Flatten(),     Dense(16, activation='relu'),     Dense(10) ])  model.compile(     optimizer='adam',     loss=tf.keras.losses.CategoricalCrossentropy(         from_logits=True     ),     metrics=['accuracy'] )  

hi, the bounding boxes. Import using numpy. Since the MNIST has no ids on images. Do not shuffle the images. Use it as it is. don’t batch unbatch and stuff.
(i have bounding_box(1).npy

each entry is an array of 4 tuples representing coordinates in 2D space [leftmost-bottommost, leftmost-topmost, rightmost-topmost, rightmost-bottommost]

set shuffle_files = False
 first 60000 images
 each entry is an array of 4 tuples representing coordinates in 2D space [leftmost-bottommost, leftmost-topmost, rightmost-topmost, rightmost-bottommost]
the bounding boxes are for first 60k images in the MNIST dataset
 update: if you have problems pairing indices of bounding boxes and images. just generate one.